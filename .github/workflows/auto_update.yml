name: Update Programming Quote

on:
  schedule:
    - cron: "0 * * * *"  # every hour (UTC)
  workflow_dispatch:      # allow manual trigger

jobs:
  update-quote:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate quote using https
        run: |
<<<<<<< HEAD
          node .github/scripts/update_advice.js
=======
          node scripts/update_quote.js
>>>>>>> 98e5994a6af52e706a01fc30e4272c25234c2106

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Updated quote - $(date '+%Y-%m-%d %H:%M')" || echo "No changes to commit"
          git push
